services:
  subtitle-web:
    build: .
    ports:
      - "${WEB_HOST_PORT:-8000}:8000"
    env_file:
      - .env.web.local
    environment:
      WEB_PORT: 8000
    volumes:
      - ./web_data:/app/web_data
    restart: unless-stopped
